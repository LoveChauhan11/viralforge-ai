# Next Actions

1. Maintainer: enable GitHub branch protection required checks (`quality`, `secrets`, `containers`, `analyze`, `dependency-review`).
2. Platform: provision Railway staging from `infra/railway/` (R-15).
3. Start Sprint 1 — `S1-01` resumable upload sessions (local MinIO).
4. Optional: ADR for error-tracking vendor (R-16) before production on-call.
5. Commit Sprint 0 work when user requests (currently uncommitted on `main`).
